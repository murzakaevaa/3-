#include <string>
#include <vector>

//Функция для поиска городов с заданным количеством пересадок
//FIX_ME: Имя переменной должно быть в lower_case_with_underscores
//std::vector<int> findCitiesWithTransfers(const std::string& fileName, int K, int L);
std::vector<int> find_cities_with_transfers(const std::string& file_name,
	int K, int L);
